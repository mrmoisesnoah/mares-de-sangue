# HANDOFF — Mares de Sangue (Mundo de Skard)

> **Documento mestre de transferência — autossuficiente.** Um novo assistente continua **sem** o histórico do chat lendo apenas: `CLAUDE.md` (raiz) + **este arquivo** + `PROCESSO-AGENTES.md` + `MIGRACOES.md`. Este HANDOFF concentra TODO o estado vivo (o que está pronto, o que falta, decisões). **Mantenha-o atualizado ao fim de cada rodada.** Aprofundamentos opcionais: `PLATAFORMA.md`, `ARQUITETURA.md`, `ROADMAP.md`, `FRONT-END-TODO.md`.

## 0. Acesso rápido (fatos que você vai usar toda hora)
- **App publicado:** https://mrmoisesnoah.github.io/mares-de-sangue  (a PLATAFORMA é publicada **na raiz**)
- **Repo (deploy):** GitHub Pages via `.github/workflows/deploy.yml` → publica a pasta `plataforma/app` na raiz. `git push` na `main` reconstrói.
- **Supabase:** projeto ref `niepiaiwusptmwepgmlq` (URL e **anon key pública** em `plataforma/app/config.js`). Postgres + Auth + Storage(bucket público `midias`) + RLS.
- **Admin/dono:** Moisés Noah — user id `c3ed4547-f032-47f6-8650-360684451acc`, `profiles.papel_global = 'admin'`.
- **App = arquivo único:** `plataforma/app/app.js` (~150KB, JS vanilla, SPA). Sem framework, sem build.
- **REGRA DE EDIÇÃO CRÍTICA:** edite `app.js`/`index.html`/`estilo.css` **só via bash+Python** (as ferramentas Edit/Write **truncam** no mount). Depois SEMPRE rode `node --check app.js` E confirme que o arquivo termina certo (`tail -1` = `init();`) e que `index.html` tem TODOS os `<script>` + `</html>`. O `index.html` já foi truncado 2x (perdeu os scripts do corpo → tela "Carregando…" infinita).

## 1. Visão Geral
**Objetivo:** plataforma própria de worldbuilding/RPG de mesa para o cenário **Mares de Sangue / Mundo de Skard** — mistura de blog + wiki + enciclopédia + diário de campanha. Preserva e expande o universo criado pelo grupo **TOGA — The Older Gods Adventures** (fundado em **17/07/2012**).
**Público:** mestres e jogadores do grupo (e leitores). Autor/admin: Moisés Noah.
**Problema que resolve:** centralizar lore, campanhas, personagens, jornais e cronologia, com visibilidade controlada (segredos do mestre).
**Estado:** EM PRODUÇÃO e em uso. Mundo de Skard já povoado e organizado. Vários mundos de exemplo (1 por tema) criados pelo autor.
**Escopo atual:** mundos, mesas (campanhas), enciclopédia, personagens+fichas, jornais, linha do tempo, sessões+recompensas, favoritos, comentários, notificações, 6 temas visuais, busca, guia, créditos.
**Escopo planejado:** editor de texto profissional (Quill), cards temáticos por entidade, painel de administração + super-admin, revisão de conteúdo do blog antigo, expandir galeria de fotos.

## 2. Princípio inegociável
**SIMPLICIDADE.** Custo zero/baixo, fácil manutenção por iniciantes, sem frameworks/dependências desnecessárias. Não reescrever a lore sem o autor (cânone é dele).

## 3. Arquitetura (resumo — ver ARCHITECTURE.md)
- **Front:** `plataforma/app/` = `index.html` (carrega supabase-js, marked.js, iconify-icon, config.js, app.js via CDN) + `app.js` (toda a lógica/telas) + `estilo.css` (tema) + `config.js` (URL+anon key, cria `sb`).
- **Roteamento:** hash (`#/rota/arg`). Estado global `S` (user, profile, mundo, mundos, mesas, view, modo...). `go(t,arg)`→`rota()`→`render()`→`layout(html)`. `esc()` escapa `& < > "`.
- **Back:** Supabase. Modelo **"tudo é publicação"**: tabela `publicacoes` com `tipo` (texto livre), `visibilidade` (publico|mesa|autor_mestre|privado|mestre), `estado` (publicado|rascunho), e FKs opcionais `mesa_id`/`personagem_id`/`jornal_id`/`sessao_id`. Segurança vive no banco (RLS) — o front só envia, não filtra permissão.
- **Ícones:** Iconify + game-icons.net (CC BY), via web component `<iconify-icon icon="game-icons:NOME">`. Função `icon(g)` + `ICONMAP[tema]` troca o símbolo por tema. `ic(semantic)` mapeia nomes semânticos. Tingidos por `currentColor` (acento do tema).
- **Temas:** `body[data-tema=...]` + variáveis CSS (`--papel,--perg,--perg2,--campo,--tinta,--tinta2,--sangue,--ouro,--borda,--sombra,--font-head,--font-body`). 6 temas: medieval, horror, lovecraft, anos80, scifi, samurai. `aplicarTema()` roda no topo de `render()` lendo `S.mundo.tema`.
- **Deploy:** push `main` → GitHub Actions publica `plataforma/app` na raiz. Site estático antigo APOSENTADO em `arquivo/`.

## 4. Estado da implementação
**Concluído e no ar:** auth, mundos, mesas, enciclopédia (explorer c/ filtro+categorias+cards/lista), mapas, linha do tempo (períodos retráteis + fundo + vínculo de conteúdo + reordenar ↑/↓), personagens (retrato redondo, ficha texto+anexo PDF, atribuir, NPC×jogador), jornais (capa de jornal + edições estilo recorte), sessões (planejamento/resumos/recompensas XP), favoritos, comentários (com avatar), notificações (sino), 6 temas com fontes/ícones próprios, toggle global cards/lista, busca, guia visual (#/guia), créditos (data TOGA 17/07/2012), mural da campanha + fixar conteúdo (limite 6), foto de personagem ampliável (lightbox), responsividade mobile (cabeçalho compacto, sem scroll lateral). **Cards temáticos por entidade (2026-06-24):** card de **mundo** = portal/arco com **astrolábio** na pedra-chave; **mesa** = **d20** (hexágono facetado); **mapa** = **pergaminho** com rolos + rosa-dos-ventos (sub-categoria do mapa vinda da 1ª tag, ex.: dungeon/cidade); **jornal** e **edição** = papel **envelhecido** (manchas, vincos, bordas rasgadas, tachinha na edição). Cores seguem o tema (--ouro/--sangue). **Campo `subtitulo` do mundo (2026-06-24):** linha curta exibida sob o card do mundo (com fallback para resumo da descrição em mundos antigos); a **descrição completa** segue no hero da home do mundo. Migration: `plataforma/migracao-mundo-subtitulo.sql`. **Favoritos** movido para acima da seção de mundos. A antiga moldura sutil por categoria (linha de topo + colchetes) foi **removida** a pedido do autor (classes `cat-*` seguem no HTML como no-op).
**Editor visual Quill no corpo da publicação (2026-06-24):** o corpo da publicação (`f_corpo` em `formPub` — usado por artigos, contos, fichas, mapas e edições de jornal) agora usa o **editor WYSIWYG Quill** (snow), carregado por CDN no `index.html` (CSS+JS `quill@2.0.2`). **Decisão de escopo:** só o corpo da publicação ganhou Quill; os demais campos (descrição de mundo/mesa, bio do perfil, história do personagem no hub `pc_corpo`, texto de evento `ev_desc`) **seguem em Markdown** (`mdEditor`) — são curtos/metadados, não justificam WYSIWYG. **Compatibilidade (não quebra o que já existe):** nova coluna `publicacoes.formato` (`md`|`html`, default `md`). Conteúdo antigo é `md` e renderiza como antes (`marked`); conteúdo novo/editado salva HTML do Quill (`formato='html'`). Render decidido por `renderCorpo(corpo,formato)`. Ao **editar** uma publicação antiga, o Markdown é convertido p/ HTML (`mdToHtmlRaw`) e ao salvar vira `html` (upgrade sob demanda, sem migração de dados). **Features preservadas:** `[[wikilinks]]` (regex aplicada também no HTML via `wikilinks()`), colar/enviar imagem (handler do Quill faz upload p/ Supabase via `uploadArquivo`), export Word, busca, resumo dos cards (`resumo`/`jornalResumo` agora removem tags HTML). Listas do Quill (markup `<ol data-list>`) são normalizadas p/ `ul/ol` padrão no salvar (`normalizaListas`) — renderizam com o CSS `.corpo` existente. CSS do editor temável em `estilo.css` (bloco `.ql-*` usando variáveis do tema). Migration: `plataforma/migracao-publicacoes-formato.sql`. **Conforto de escrita (2026-06-24):** editor inline mais alto (min 360px) e com **borda arrastável** (`resize:vertical`); botão **⛶ Tela cheia** (`toggleFoco`) que expande o editor em modo foco (largura de leitura ~860px, fundo do tema; sai com o botão ou **Esc**, trava o scroll do body via `body.foco-lock`). **Colar inteligente:** texto do **Word/docx** mantém formatação (Quill lê `text/html` do clipboard); **Markdown** colado como texto puro é detectado (`pareceMarkdown`, sem falso-positivo em prosa) e convertido via `mdToHtmlRaw`; imagem colada faz upload. Tudo no handler de `paste` do `initQuills`. **Layout fluido + imagem ajustável (2026-06-24):** `.layout`/`.rodape` ampliados p/ 1320px e `.form` agora fluido (`max-width:min(1040px,100%)`) — formulário acompanha o tamanho da janela; modo foco responsivo (`min(1150px,94vw)`); editor inline mais alto (440/420px). **Controle de imagem:** clicar numa imagem no editor mostra (a) uma **alça de redimensionar** (`img-handle`/`imgDragStart`) no canto inferior direito — arrastar livremente ajusta a largura em px mantendo proporção (clamp 40px..largura do editor; sincroniza no model via `formatText` no soltar); e (b) um popover (`img-pop`/`initImgTools`) com **Tamanho** preset (P 30% / M 50% / G 75% / Total 100% — grava o atributo `width`) e **Posição** (esquerda/centro/direita via `align` nativo do Quill). CSS `.corpo .ql-align-*` faz o alinhamento valer também na renderização. `.ql-editor img{max-width:100%}` mantém a imagem responsiva ao texto. **Alça de arrastar:** `img-handle`/`imgDragStart` redimensiona livremente (largura px, proporção mantida). **Colar preserva mais formatação (2026-06-24):** atributores de **estilo inline** registrados (`size, align, direction, color, background, font` via `Quill.register(attributors/style/*, true)`) — cor, realce, tamanho, fonte e alinhamento sobrevivem ao colar (Word/Docs/web) e renderizam idênticos fora do editor (estilo inline, não classe). Barra ampliada: + tachado, cor, realce, alinhamento. **Autosave de rascunho (localStorage):** `salvarRascunho`/`draftKey` (chave por usuário+hash da rota) grava o corpo a cada ~0,8s de edição do usuário; ao reabrir o formulário, `mostrarBarraRascunho` oferece **Recuperar/Descartar**; `limparRascunho` no salvar com sucesso. Cobre o caso de F5/erro antes de salvar. **Contorno de imagem (float):** popover ganhou linha **Contorno** (`imgContorno`): esquerda/direita com texto fluindo ao redor, centralizar, sem contorno — aplica `style` inline na `img` (persiste no HTML salvo, renderiza em `.corpo`; clearfix `.corpo::after/.ql-editor::after`). **Arrastar imagem com o mouse — modelo "drop on release" (fluido)** (`initImgMove`/`caretIdxDePonto`/`caretRectDePonto`): a 1ª tentativa de reflow-ao-vivo (mover o embed a cada `mousemove` via `silent`) ficou **travada/com jitter** e foi abandonada. Agora, durante o arraste, o documento **não** é alterado: a imagem de origem fica esmaecida (`.img-arrastando`, opacity ~.32), uma **sombra** (`.img-ghost`) segue o cursor e uma **linha de destino** vertical (`.img-drop-line`) marca onde vai cair (via `caretRectDePonto`). O move acontece **uma única vez no `mouseup`** (`deleteText`+`insertEmbed`, preservando `width`/`style`, com fallback `getLeaf(idx+1)`→`getLeaf(idx)`). Imagens recebem `draggable=false` + `dragstart` cancelado (sem DnD nativo). A alça do canto (`img-handle`) continua só para redimensionar. (`initImgDrag` antigo = código morto.) **Toolbar de imagem redesenhada:** popover `img-pop` agora tem cabeçalho, botões `.ipb` estilo editor (Tamanho/Contorno/Alinhar) e ação **Excluir** (`imgExcluir`, lixeira game-icons) numa faixa de ações. **➡ SUBSTITUÍDO (2026-06-24) pela biblioteca `@enzedonline/quill-blot-formatter2@3.2.0`** (CDN: CSS+`dist/index.js` no `index.html`; global `window.QuillBlotFormatter2`). Padrão Word/Blogger, **validado ao vivo no Chrome do autor** (carrega, registra `modules/blotFormatter2` no Quill 2.0.2, instancia, e ao clicar na imagem cria overlay + 4 alças de cantos + toolbar com Align Left/Center/Right, Link e Alt/Title). Registrado e configurado em `initQuills` (`__mods.blotFormatter2={ resize:{ useRelativeSize:true, minimumWidthPx:40 } }`, defensivo com try/catch). **Exclusão = teclado** (selecionar a imagem e Delete/Backspace) — não há botão de excluir na toolbar da lib. **Serialização:** alinhar embrulha a imagem em `<span class="ql-image-align-left|center|right" style="--resize-width:NN%">`; largura relativa (%) = responsiva. CSS replicado para `.corpo` (regras `.corpo [class^="ql-image-align-"]` + variáveis de margem) para renderizar igual fora do editor. As funções manuais antigas (`initImgTools`/`initImgMove`/`img-pop`/`img-handle`/`imgExcluir`/`imgContorno`) ficaram **sem uso** (não são mais chamadas) — limpar numa faxina futura. **Botões custom na toolbar (2026-06-24, validado ao vivo no Chrome):** **Excluir** e **Cortar** adicionados via `criarBfSpec` — subclasse `MdsImageSpec extends ImageSpec` cujo **construtor reatribui o campo de instância `getActions`** (no blot-formatter2 `getActions` é uma *arrow function de instância*, NÃO método de protótipo — overrides no protótipo são ignorados; tem que reatribuir `this.getActions` após `super()`). As ações `ExcluirAction`/`CortarAction extends BF.Action` expõem `toolbarButtons=[new ToolbarButton(nome,handler,f.options.toolbar)]`; ícones SVG passados em `toolbar.icons` (merge com `BF.DefaultOptions.toolbar.icons` via `BF_ICONS`). Excluir = `deleteText` no índice do blot. **Cortar** = `abrirCorteImagem(img,q)`: modal (reusa `.crop-ov/.crop-box/.crop-acts`; novas `.crop-stage/.crop-sel/.crop-head`) onde o autor arrasta um retângulo; recorta via **canvas** (`drawImage`+`toBlob`, **CORS do Supabase OK — validado ao vivo**), faz **reupload** (`uploadArquivo`) e substitui o embed preservando atributos (Delta `getContents`/`updateContents`). **Mover = arrastar reativado:** `initImgMove(q)` voltou a ser chamado; convive com o blot-formatter porque, quando a imagem está selecionada, o overlay cobre a `<img>` (mousedown não cai na img); em imagem não-selecionada, arrastar >5px mostra sombra+linha e move no `mouseup` via **Delta** (`getContents`+`deleteText`+`updateContents`), preservando align/width. Clique simples (sem arrasto) passa para o blot-formatter selecionar. **Rascunhos por mundo (2026-06-24):** a chave do rascunho local agora inclui o mundo (`mds:draft:<uid>:<mundoId>:<hash>`); `rascunhosLocais` filtra pelo mundo atual e `meusRascunhos` aplica `.eq("mundo_id",S.mundo.id)` quando há mundo selecionado — rascunhos não vazam mais entre mundos. Resumir um rascunho local usa a **chave explícita** (`window.__autoRestoreKey`) em vez de recalcular `draftKey`. **Área de Rascunhos (combinada):** link **Rascunhos** na lateral (mundo e home) + seção **Seus rascunhos** na home; rota `rascunhos`/`telaRascunhos` lista (a) rascunhos do **banco** (`meusRascunhos` = `publicacoes` do autor com `estado='rascunho'`) e (b) rascunhos **locais não salvos** (`rascunhosLocais` lê chaves `mds:draft:*` do localStorage; `resumirRascunhoLocal` reabre a rota e a barra de recuperação aparece; `descartarRascunhoLocal` apaga). **Aviso ao sair:** `beforeunload` avisa se houver edição não salva (`window.__editDirty`, marcado no `text-change` do usuário, zerado em `render()` e ao salvar). **Correções 2026-06-24:** item **Jornais** da lateral não fica mais vermelho (removida classe `destaque`, agora usa `on()` como os demais); **menu lateral mobile** agora fecha ao clicar fora (handler global de clique passou a remover `.lateral.aberta` quando o clique não é no `#btn-menu` nem dentro da lateral).
**Atenção imediata (pendente de ação do autor):** migration `migracao-publicacoes-formato.sql` JÁ RODADA e push do Quill JÁ FEITO pelo autor. **Falta push da correção do corte de imagem (2026-06-25)** — `plataforma/app/app.js` + `plataforma/app/estilo.css`. Depois: **validação ao vivo** nos 6 temas (criar/editar publicação, imagem colada, wikilink, lista) — combinado de fazer após refinar a imagem. A migration `migracao-excluir-mundo-mesa.sql` já foi rodada pelo autor.
**Pendente (tarefas abertas, em ordem sugerida):** (1) **painel de Administração + super-admin**; (2) revisão do conteúdo do blog antigo; (3) excluir a mesa de teste "Sombras Vindas do Tempo" (`plataforma/limpeza-mesa-sombras.sql`); (4) avaliar (opcional) promover a "Perfil / história" do personagem (`pc_corpo`) ao Quill se ficar longa demais; (5) **[PRÓXIMO HANDOFF — DISCOVERY] Validação estrutural / legibilidade do código:** o autor quer pensar em deixar o código mais fácil de um dev abrir e ler — avaliar separar `app.js` (~arquivo único grande) em módulos/componentes, melhorar comentários e organização. Discovery (trazer opções: ES modules, divisão por telas, JSDoc, etc.), sem reescrever de cara; manter a simplicidade. (6) **[PRÓXIMO HANDOFF — DISCOVERY] Escala de usuários e mundos:** o autor quer pensar (discovery, ainda sem implementar) na melhor forma de organizar a plataforma para quando houver **muitos usuários e muitos mundos** — facilitar a vida de quem busca o próprio mundo e de quem quer **descobrir/conhecer outros mundos** (ex.: busca/filtro de mundos, categorias/tags de mundos, destaque/curadoria, mundos públicos vs privados, paginação, "meus mundos" vs "explorar"). Trazer opções e trade-offs no próximo handoff antes de codar.

## 5. Regras de negócio
- **Visibilidade** (campo `visibilidade`): `publico` (todos) · `mesa` (membros da campanha) · `autor_mestre` · `privado` (só autor) · `mestre` (só mestre da mesa). + `estado` `rascunho` (só autor vê) / `publicado`. É como o mestre prepara segredos.
- **Papéis:** `profiles.papel_global = 'admin'` (global). Por mesa: `mesa_membros.papel` = `mestre`|`jogador`. Mestre = `mesas.mestre_id`. Dono do mundo = `mundos.dono_id`. Colaboradores de mundo (tabela própria) podem criar conteúdo.
- **Pedidos de acesso:** usuário pede acesso a mundo/mesa/personagem → dono é notificado → aprova.
- **Mural da Campanha:** conteúdo player-facing da mesa + fixados (`mural_pins`), **limite 6** fixados/mesa; só mestre fixa/desfixa.
- **Ficha de personagem:** é uma `publicacao` `tipo='ficha'` ligada ao personagem (visibilidade própria) + anexo opcional (`arquivo_url`).
- **Cânone (NÃO alterar sem o autor):** **Exar Khun** (não "Kun"); **Dranor** = reino, **Dranorak** = capital; **Gaurhoth** (não "Gauroth"). Em dúvida de grafia, usar a original de Tolkien.

## 6. Fluxos de usuário
Os fluxos principais (entrar → escolher mundo → navegar enciclopédia/linha do tempo/personagens/jornais/mapas; mestre prepara conteúdo com visibilidade controlada; jogador contribui) são autoexplicativos na própria UI (`#/guia` traz o guia visual). Detalhe de telas: ler as funções `tela*` em `plataforma/app/app.js`.

## 7. Contexto estratégico
- Visão do autor: preservar/expandir o universo TOGA com uma ferramenta bonita, simples e barata, que mestres e jogadores usem de verdade.
- Prioridade declarada (ordem): **quick wins** (reordenar eventos ✓, fotos ✓, cards temáticos ✓) → **editor Quill** ◀ próximo → **blog antigo**. Mais: painel admin/super-admin.
- O autor **não é dev** — pediu que decisões técnicas sigam o **PROCESSO-AGENTES.md** (N1–N6 + Qualidade) e que o assistente decida quando ele não souber.
- **NÃO alterar sem validação:** o cânone/lore; a estrutura de visibilidade (RLS); o deploy (publica `plataforma/app` na raiz).

## 8. Tarefas abertas e decisões
Tarefas abertas: ver a lista **Pendente** na seção 4. Decisões técnicas recentes: cards temáticos por entidade implementados em CSS+JS no próprio `app.js`/`estilo.css` (formas via `clip-path`/SVG inline, sem libs); edições no app **só via bash+Python** (Edit/Write truncam). Dívida técnica: classes `cat-*` viraram no-op após remover a moldura sutil — podem ser limpas numa faxina futura.

## Conhecimento implícito / lições
- **Truncamento no mount:** Edit/Write truncam arquivos grandes; use bash+Python e VERIFIQUE integridade (não só sintaxe — teste a carga). `node --check` não pega erro de runtime no topo (ex.: `var X=funcQueLeVarAindaIndefinida()`).
- **PostgREST:** após criar função, `notify pgrst, 'reload schema';` senão `rpc()` dá PGRST202.
- **Migrations idempotentes** (`create or replace`, `drop policy if exists` antes de `create policy`, `if not exists`). Rodar na ordem de `MIGRACOES.md`.
- **SECURITY DEFINER** precisa `set search_path = public`.
- **Preferência do autor:** respostas concisas; validação ao vivo no Chrome ao fim de cada bloco (não a cada mudança); ele sobe (git push) e roda migrations e avisa "subiu/rodou".
- **Não excluir, arquivar** (quando ele pede p/ tirar coisas — mover, não apagar; exceto conteúdo de teste que ele autorizou excluir).

---
_Última rodada (2026-06-24): **editor Quill (WYSIWYG)** no corpo da publicação, compatível com o Markdown antigo (coluna `publicacoes.formato`, render por `renderCorpo`). Só o corpo de publicação ganhou Quill; demais campos seguem Markdown. SQL a rodar: `migracao-publicacoes-formato.sql`. Funções puras testadas no node (wikilink/renderCorpo/resumo/jornalResumo OK); falta validação ao vivo após push+migration. Migrations de subtítulo já rodadas pelo autor. Iterações posteriores: colar preserva formatação (estilos inline registrados); autosave/recuperar rascunho (local) + **área de Rascunhos** (lateral seção Criar + home, banco+local, por mundo); imagem com redimensionar (alça), contorno/float, e **arrastar com o mouse refluindo ao vivo** (`initImgMove`, com guarda de jitter via `elementFromPoint` e fallback de re-aquisição de nó); botão **Tela cheia** destacado; correções na lateral (Jornais sem vermelho; fecha ao clicar fora). **Processo:** novo papel **PO (Product Owner)** adicionado ao `PROCESSO-AGENTES.md` (capta a voz do usuário e alinha estratégia com o Líder de Entrega/Qualidade). Revisão holística feita por subagente: sem bloqueadores._

_Última rodada (2026-06-25): **correção do corte de imagem (`abrirCorteImagem`)**. **Sintoma:** botão Cortar abria modal sem a ferramenta — área vazia e só o aviso "arraste sobre a imagem". **Causa (diagnosticada ao vivo no Chrome):** o cortador sempre acrescentava cache-buster `?cb=<ts>` ao `src` e carregava com `crossorigin="anonymous"`. Isso (1) **corrompe URLs `data:`/`blob:`** (imagens arrastadas de pasta local p/ o editor viram base64 embutido) → `onerror` → modal vazio; e (2) **falha em imagens externas sem CORS** (`blogger.googleusercontent.com` do blog antigo). Imagens do **Supabase** (com CORS) sempre funcionaram. **Fix:** reescrito com estado carregando/erro + `onload`/`onerror`; NÃO acrescenta `?cb=` a `data:`/`blob:`; botão Cortar começa desabilitado e só habilita quando a imagem carrega; para http(s) que falhe no load direto, **fallback automático via proxy gratuito `images.weserv.nl`** (`?url=ssl:<host/path>`) que reserve com CORS → canvas/corte funcionam e o recorte é re-hospedado no Supabase (`uploadArquivo`); se nem o proxy carregar, mensagem clara. Proxy aprovado pelo autor (só fallback p/ externas). CSS `.crop-load`/`.crop-erro` em `estilo.css`. **Validado ao vivo (injeção na página):** `data:` ✅, externa via proxy ✅ (inclui canvas export), URL quebrada ✅ erro tratado. **A FAZER:** push de `app.js`+`estilo.css` e validação ao vivo nos 6 temas. **Recomendação p/ próxima rodada (não feito):** drop de imagem local insere base64 gigante no corpo (incha o HTML); ideal o handler de **drop** subir p/ Supabase como o de **paste**. **Arraste fluido tipo Blogger:** mantido o modelo atual "drop-on-release" (sombra+linha de destino) — refluxo síncrono a cada `mousemove` no Quill causa jitter/trava (já tentado e abandonado); autor aceitou manter como está._

_Última rodada (2026-06-25, parte 2): **(A) não dava p/ arrastar a imagem depois de cortar.** Causa: o corte é acionado com a imagem **selecionada** pelo blot-formatter; a **moldura/overlay do blot-formatter cobre a `<img>`** (comprovado ao vivo: `elementFromPoint` no centro da imagem retorna o DIV do overlay, não a IMG), e o `initImgMove` só inicia o arraste quando o `mousedown` cai na própria IMG. Após o corte a imagem ficava nesse estado selecionado. **Fix:** ao aplicar o corte, além de inserir a nova imagem, agora faz `q.setSelection(i+1)` + `ocultarBlotFormatter(q)` (novo helper que chama `q.getModule("blotFormatter2").hide()`), repetido em `setTimeout(60ms)` — devolve a imagem ao estado "não selecionado" e o arraste volta a funcionar (mecanismo validado ao vivo: com a moldura oculta, `topIsImg=true` e o move por índice funciona). **(B) Drop de imagem local agora sobe p/ Supabase** (pedido do autor): novo `initDropUpload(q)` (chamado no `initQuills` após `initImgMove`) intercepta o `drop` de arquivos de imagem (capture + `stopPropagation` p/ ganhar do handler nativo do Quill), faz upload via `uploadArquivo` (`quillSubirImgEm` insere placeholder "enviando imagem…" e troca pela `<img>` do Supabase no índice do drop via `caretIdxDePonto`) — acaba com o base64 gigante embutido no corpo. **(C) Timeout no cortador:** se a imagem não carregar em 15s, mostra erro em vez de ficar preso em "Carregando…" (visto num caso de URL que pendura). **Push feito e VALIDADO AO VIVO PELO AUTOR (2026-06-25):** cortar → arrastar a imagem cortada OK; drop de arquivo local vira URL do Supabase (não base64) OK. Correção do corte (data:/proxy/timeout) e deseleção pós-corte confirmadas em uso._

_Última rodada (2026-06-30): **Recompensas (Resumo + Detalhes + Fixar) e Hero colapsável da home — AGORA REALMENTE NO CÓDIGO.** ⚠️ Atenção: o `HANDOFF-2026-06-30.md` (raiz) descrevia esse trabalho como concluído, mas o código anterior **não tinha as mudanças de recompensas** (form ainda com "Quantidade de XP"+"Observação"; card não-clicável injetando a descrição inline). Reimplementado de fato em `plataforma/app/app.js` + `estilo.css` via bash+Python. **Recompensas:** `formRecompensa` ganhou campo **Resumo** (`rc_resumo`, linha curta, max 100) e renomeou "Observação"→**Detalhes** (Quill); campo **Quantidade de XP removido** (`rc_qtd` some do form e `salvarRecompensa` não lê mais `quantidade` — coluna preservada no banco). `renderRecsHTML` agora exibe **resumo (com título como sub-linha quando diferem)**, **card clicável** → `go('recompensa',id)`, ações do mestre (fixar/editar/excluir) com `stopPropagation`; **sem** quantidade e **sem** descrição inline no card. Nova **`telaRecompensa(id)`** (rota `recompensa`): resumo destacado + tipo/personagem + **Detalhes** renderizados por `renderCorpo` (detecta html/md) + ações do mestre. **Fixar no mural:** `fixarRecompensaNoMural(recId,mesaId,jaFixado,btnEl?)` faz toggle em `mural_pins` com `tipo='recompensa'` (limite **6 unificado por mesa** — `confirmarPin` passou a contar TODOS os pins, não só `publicacao`); `muralItem(p,mesaId,podeDespinar,kind)` e `despinarMural(mesaId,alvoId,tipo)` agora suportam `kind/tipo='recompensa'`; `telaMesa` carrega pins de recompensa e os renderiza no mural (link p/ a tela de detalhe). **SQL:** só `migracao-recompensas-resumo.sql` (coluna `resumo`) — `mural_pins.tipo` é texto livre sem CHECK e a RLS de insert/delete já é `is_mestre or is_admin`, então fixar recompensa **não exige migration nova**. **Hero da home colapsável:** `hero()`/`toggleHeroDesc(btn)` refeitos — descrição > 160 chars começa colapsada (CSS `max-height:7.4em` desktop / `5.2em` ≤640px) com botão **"Leia mais ▾ / Recolher ▴"** acessível (`aria-expanded`/`aria-controls`); some o problema do texto ocupar a tela toda no mobile. **Verificação:** `node --check` OK, `tail -1`=`init();`, `index.html` íntegro (7 `<script>` + `</html>`), CSS balanceado (delta), smoke-test em node das funções de render passou (resumo no card, colapso do hero, pin on). **Falta:** validação ao vivo nos 6 temas + mobile pelo autor, e rodar `migracao-recompensas-resumo.sql` no Supabase se ainda não rodou._

_Última rodada (2026-06-30, parte 2): **redesign do card de Recompensas + botão de modo no mobile (PROCESSO-AGENTES).** **(A) Recompensas viraram cards** (antes era linha de `lista2`/`rec-li` apertada, com resumo e título grudados). `renderRecsHTML` agora gera `<div class="rec-cards">` (grid `minmax(280px,1fr)`, 1 coluna no mobile) de `.rec-card`: ícone do tipo (⭐/⚔️/🏆), **resumo** em destaque, **título** como subtítulo (só quando difere do resumo), e linha de **chips** (`.rec-chip-tipo` XP/Item/Prêmio + chip do destinatário "Todos / geral" ou nome do personagem). Ações do mestre (`.rec-pin` fixar + `.rec-mini` editar/excluir) empilhadas à direita com `stopPropagation`; card todo clicável → `telaRecompensa`. **(B) Mural:** `muralItem` para recompensa ganhou o emoji do tipo antes do rótulo "Recompensa" (classe `mural-rec`), mantendo o cartão do mural existente. **(C) Botão de modo (cards/lista) no mobile:** `modoBtnHTML` agora separa `<span class="modo-ic">` (☰/▦) de `<span class="modo-tx">` (Lista/Cards); CSS esconde `.modo-tx` em `@media(max-width:600px)` e aumenta o ícone — no celular fica só o ícone (sem o texto "LISTA"/"CARD" que ficava feio). `aria-label` descreve a ação para leitores de tela. **CSS:** bloco "Recompensas — cards" em `estilo.css` (após `.rec-pin.on`), `.rec-cards` somado à regra mobile de 1 coluna, `.modo-btn` virou flex. **Verificação:** `node --check` OK, `tail -1`=`init();`, `index.html` íntegro (7 `<script>`+`</html>`), CSS sem nova imbalância (delta de chaves inalterado), smoke-test em node (cards balanceados, estado fixado, subtítulo, destinatário, botão nos 2 modos, mural rec) passou. **Falta:** validação ao vivo nos 6 temas + mobile pelo autor. Sem migration nova. **(D, mesma rodada) Identificador da sessão na recompensa:** a recompensa agora mostra a qual **sessão** pertence — na **tela de detalhe** (`telaRecompensa` busca `umaSessao(r.sessao_id)`; o título da sessão vira o link do breadcrumb "‹ <título>" e aparece numa linha `.rec-det-ses` com ícone de d20 abaixo do tipo/destinatário) e no **mural** (`telaMesa` faz um `select id,titulo from sessoes in(sessao_ids)` das recompensas fixadas e anexa `rc._sesTit`; `muralItem` renderiza `.mural-rec-ses` com o título da sessão). Na lista de recompensas dentro da própria sessão não se repete (já é o contexto). CSS `.rec-det-ses`/`.mural-rec-ses` em `estilo.css`. Sem migration. Verificado: `node --check` OK, smoke-test do mural com/sem sessão OK. **(E) Sessão também no card da recompensa:** `renderRecsHTML` recebe `sesTit` (passado por `telaSessao` = `se.titulo`) e exibe um chip `.rec-chip-ses` (ícone d20 + título da sessão) na meta de cada card — assim a recompensa "carrega" o identificador da sessão também na lista da sessão, não só no mural/detalhe. Verificado em node (chip presente com sessão / ausente sem). _

_Última rodada (2026-06-30, parte 3): **redesign visual do card de Recompensas + cache-busting.** O autor achou o card da área da sessão "texto solto na tela" (o do mural ficou bom). **Diagnóstico:** o CSS publicado JÁ tinha `.rec-card` (confirmado via fetch do estilo.css no ar) — o sintoma (chips colados "XPTodos/geral", sem moldura) era **cache antigo do `estilo.css`** no navegador (app.js novo carregou, css velho ficou). **Correções:** (1) `index.html` agora versiona os assets — `estilo.css?v=20260630b` e `app.js?v=20260630b` (bump esse valor a cada mudança de css/js para furar cache). (2) Redesign do `.rec-card` para a riqueza visual do card do mural: `background:var(--perg2)`, borda `--ouro`, **lombada `--sangue`** (`::before` 5px à esquerda), **medalhão** do ícone (círculo 44px `--papel` com anel `--ouro`), resumo em `--font-head` 16px/600, subtítulo itálico, e **chips fortes** — `.rec-chip` fundo `--papel`+borda; `.rec-chip-tipo` pílula sólida `--ouro` com texto branco; `.rec-chip-ses` borda ouro + texto sangue. **Verificação:** `node --check` OK, `tail -1`=`init();`, index.html íntegro (7 script+</html>), CSS delta de chaves inalterado. Preview aprovado em mockup (cores reais do tema). **Falta:** push + validação ao vivo nos 6 temas/mobile._

_Última rodada (2026-06-30, parte 4): **cabeçalho mobile repensado + cache busting v=c.** **Diagnóstico do "não refletiu":** o `estilo.css` no ar JÁ tinha o redesign (confirmado por fetch da URL versionada `estilo.css?v=20260630b` — tem `.rec-card::before`, medalhão, `.modo-tx{display:none}` na media mobile). O sintoma do autor (card "cru", e botão de modo mostrando ícone+texto **empilhados** no mobile) é o app.js novo (que separa `<span class="modo-ic">`/`<span class="modo-tx">`) rodando com **estilo.css velho em cache do navegador** (sem o `flex` no `.modo-btn` nem a regra que esconde `.modo-tx`) → os dois spans quebram linha e o texto aparece. **Correções:** (1) **versão dos assets bumpada para `?v=20260630c`** em `index.html` (css+js) — URL nova que nenhum cache tem, força fetch limpo (inclui mobile, onde não há Ctrl+F5). (2) **Cabeçalho mobile redesenhado:** `.modo-btn` no mobile vira **ícone quadrado** (`padding:7px 9px;font-size:0` + `.modo-ic` 18px; texto escondido), no mesmo estilo do sino; a **pílula do mundo cresce** (`.ws-wrap{flex:1 1 auto;min-width:0}` + `.world-switch{max-width:none;width:100%}`) ocupando o espaço livre e mostrando muito mais do nome (antes truncava em "O Mundo …" por causa de `max-width:118px`). Removido o `max-width:140px` conflitante da media de linha 158. **Verificação:** CSS delta de chaves inalterado, index.html íntegro (7 script+</html>), preview antes/depois aprovado. **Falta:** push + Ctrl+Shift+R (ou aba anônima p/ verificar na hora); no mobile o `?v=c` resolve ao recarregar. _

_Última rodada (2026-06-30, parte 5 — CAUSA RAIZ): **o card de recompensa cru NÃO era cache — era uma `{` órfã no `estilo.css`.** Sintoma: na **tela de sessão** o `.rec-card` não recebia estilo nenhum (parecia texto solto), mesmo em aba anônima, enquanto o mural e os cards de "Resumos" da mesma página estavam OK. **Diagnóstico:** rastreador de chaves (ignorando strings/comentários) acusou delta +1 e **uma chave aberta sem fechar na linha 684**: a regra `.crop-stage{ position:relative; display:inline-block; max-width:80vw; max-height:66vh;` ficou **truncada** (sem `}` e sem as regras irmãs `.crop-stage img`/`.crop-load`/`.crop-erro`/`.crop-sel` — provável vítima do truncamento de mount mencionado neste doc). Com a regra aberta, o navegador **engolia todo o CSS após a linha 684** numa "declaração" inválida — e o bloco `.rec-card`/`.rec-cards`/`.rec-chip` (inserido logo depois) caía justamente nessa zona morta. Tudo **antes** da 684 (mural, `.card`, cabeçalho mobile) funcionava; tudo **depois** não. **Fix:** fechei `.crop-stage` e **reconstruí** as regras perdidas do cortador (`.crop-stage img`, `.crop-load`/`.crop-erro`, `.crop-sel`). CSS agora **balanceado (delta 0)**, sem chave órfã (verificado). Bônus: o modal de **corte de imagem** volta a ter estilo (estava quebrado desde a truncagem). Versão dos assets → `?v=20260630d`. **Lição reforçada:** ao editar `estilo.css` via bash+Python, conferir SEMPRE o **balanço de chaves** (`{`==`}`) — uma regra truncada no meio do arquivo mata silenciosamente todo o CSS subsequente. **Falta:** push + validar ao vivo (card de recompensa na sessão deve aparecer com moldura/medalhão/chips; conferir o cortador de imagem nos 6 temas). _

_Última rodada (2026-06-30, parte 6 — Pedidos/permissões de mesa + DISCOVERY de convites): **(A) Botão "Pedir acesso a esta mesa" só para quem faz sentido.** Sintoma do autor: ao clicar "Pedir acesso" aparecia sempre "Você já tem um pedido (aprovado) para este item." Causa: o botão (em `telaMesa`) só checava `!ehM` (não-mestre), então um jogador **já aprovado/membro** continuava vendo o botão e reincidia no pedido. Fix: `telaMesa` agora carrega `souMembro` (`mesa_membros` do usuário) e `meuPedido` (estado do último pedido); `var podeCriar=ehM||souMembro;`. O botão só aparece para `S.user&&!ehM&&!souMembro`; com pedido **pendente** mostra aviso "⏳ Acesso solicitado — aguardando o mestre aprovar" (sem reenvio). **(B) Notificação de pedido leva à área de aprovação.** Antes `abrirNotif` mandava o dono para a página pública da mesa. Agora notificações `tipo==="pedido"` roteiam: mesa→`editarMesa`, personagem→`personagem`, mundo→`editarMundo` (quando o mundo está aberto; senão toast orientando). `pedirAcesso` passou a anexar `tipo,alvoId` também no caso de mundo (antes ia `null`). **(C) Brecha de permissão fechada.** A RLS `pers_insert` só exigia `jogador_id=auth.uid()` → qualquer autenticado criava personagem em **qualquer** mesa. Nova `migracao-personagem-membro.sql` (idempotente) exige `is_membro(mesa_id)` para personagem ligado a mesa (livre no mundo segue liberado; dono/admin idem). `schema.sql` sincronizado. Publicações já eram gated por `is_membro` (pub_insert) — OK. No front, os botões de criar na mesa (+ Conteúdo, + Personagem, + Postar, + Mapa) agora usam `podeCriar` (some para não-membros, evita erro de RLS). **Verificação:** `node --check` OK, `tail -1`=`init();`, `index.html` íntegro (7 `<script>`+`</html>`), 8/8 replaces aplicados 1x cada. Assets bumpados `?v=20260630e`. **Falta:** push de `app.js`+`index.html`+`schema.sql`+migration, **rodar `migracao-personagem-membro.sql`** no Supabase, e validação ao vivo (pedir acesso como 2º usuário; aprovar pela notificação; tentar criar personagem como não-membro deve falhar)._

_**(D) DISCOVERY aberto — GRANDE IMPLEMENTAÇÃO p/ próximo handoff: Área de usuários + Convites (escala).** Pedido do autor: com 100–1000 usuários/mesas, (1) jogador não vê as mesas e não tem como sinalizar; (2) mestre não acha o jogador no `<select>` de todos (nomes repetidos); (3) falta canal direto mestre⇄jogador (sem chat). Proposta completa em **`docs/DISCOVERY-usuarios-convites.md`** + mockup **`docs/prototipo-usuarios-convites.html`**. Resumo da recomendação: campo **`apelido`** no perfil (vira nome exibido) + **código de usuário** copiável (`U-XXXXXX`, derivado do id) para desambiguar; **busca** do mestre por apelido/código (função `buscar_usuarios` SECURITY DEFINER, sem e-mail, com limit) no lugar do `<select>`; **convites** mestre→jogador (tabela `convites` + `aceitar_convite` RPC, pois o jogador não é mestre p/ inserir em `mesa_membros`) com Aceitar/Recusar no sino e página `#/convites`; **solicitação** jogador→mestre segue via `pedir acesso` (mesa pública ou link/código da mesa). Faseamento: 1) apelido+código; 2) busca do mestre; 3) convites; 4) (opcional) código/link de convite da mesa. **Pendente de decisão do autor** (ver §8 do discovery): identificação apelido+código vs @handle único; apelido como nome exibido; não permitir busca por e-mail; profundidade inicial (Fase 1+2 ou já 3). Trazido como protótipo para iniciar definições antes de codar._

_Adendo (2026-06-30, parte 6): **(E) Notificar o solicitante quando o pedido é aprovado.** `aprovarPedido` agora chama `notificar(uid,"aprovado","Seu pedido de acesso foi aprovado! Você já pode acessar.",tipo,alvoId)` (link nulo p/ mundo). Tipo `"aprovado"` (≠ `"pedido"`) cai no roteamento normal do sino → clicar leva o usuário direto à mesa/personagem. Assets `?v=20260630f`. `node --check` OK. (Recusa segue sem notificar — não pedido; o botão "Pedir acesso" reaparece já que o pedido deixa de estar "pendente".)_

_Última rodada (2026-06-30, parte 7 — validação e correção do fluxo de pedido de acesso): bugs do fluxo ATUAL (não do recurso de convites, que segue só em discovery). **(1) Sino não atualizava** — `carregarNotifs` só rodava no login (sem polling/realtime), então o badge de nova notificação nunca aparecia em sessão aberta. Adicionados `pintarSino()` (atualiza o contador `.sino-ct` e, se aberto, a lista, sem re-render completo) e `iniciarPollNotifs()` (poll de 45s, guard `window.__notifPoll`, iniciado em `carregar()`); `toggleNotif` virou async e **recarrega as notificações ao abrir** o sino; `marcarTodasLidas`/`abrirNotif` chamam `pintarSino`. **(2) Mestre não via o pedido "na área da mesa"** — `renderPedidos` só aparecia em Editar mesa/mundo/personagem. Agora `telaMesa` mostra a caixa de pedidos pendentes para o mestre (`<div id="pedidosmesa-inline">` + `renderPedidos("mesa",id,...)`), com aprovar/recusar ali mesmo; CSS `.secao-ped`. `abrirNotif` para pedido de **mesa/personagem** agora cai no roteamento padrão (mesa→página da mesa, onde está a caixa; personagem→página com `pedidospers`); só pedido de **mundo** vai a `editarMundo`. `aprovarPedido`/`recusarPedido` refrescam a caixa inline; aprovar dá toast "Acesso aprovado." **(3) `telaMesa` robusta** — se a mesa não está em `S.mesas` (clique de notificação vindo de outro contexto), busca do banco antes de desistir. **(4) Adicionar jogador manualmente** (`addJogador`) agora **notifica** o usuário ("Você foi adicionado à mesa X", link p/ a mesa) + toast. RLS `ped_select` já permitia o mestre ver os pedidos (is_mestre), então não houve migration. **Verificação:** `node --check` OK, `tail -1`=`init();`, `index.html` íntegro (7 script+</html>), CSS delta 0. Assets `?v=20260630g`. **Falta:** push e validação ao vivo (2 contas): pedir acesso → badge aparece p/ o mestre em ~45s ou ao abrir o sino → clicar a notificação cai na mesa com a caixa de pedidos → aprovar → o solicitante recebe "aprovado" e vira membro._


_Última rodada (2026-07-01): **Área de usuários — apelido + busca por e-mail/apelido + convites (Fases 1–3 do DISCOVERY-usuarios-convites, implementadas).** Decisão do autor: convidar por **e-mail exato** (identificador que ele já conhece — dispensa código `U-XXXX`) + **apelido** como nome de exibição e busca amigável; e-mail **nunca** é listado/retornado (privacidade tipo Instagram). **Banco (`migracao-usuarios-convites.sql`, idempotente, A RODAR):** coluna `profiles.apelido` (+índice); `buscar_usuarios(termo)` SECURITY DEFINER (e-mail = match exato e não retornado; apelido/nome por prefixo; limit 20; só autenticado); tabela `convites(mesa_id,convidado_id,criado_por,estado,unique(mesa,convidado))` + RLS (convidado/mestre/admin veem; mestre insere/cancela; convidado atualiza o próprio); `aceitar_convite(p_convite)` (insere em `mesa_membros` via DEFINER, pois jogador não pode) e `meus_convites()` (lista pendentes já com nome de mesa/mundo/convidante, contornando a RLS de mesas que o convidado ainda não vê). Termina com `notify pgrst,'reload schema';`. `schema.sql` e `docs/MIGRACOES.md` (item 23) sincronizados. **Front (`app.js`, +6,6KB):** helper `exib(p)=apelido||nome||'Aventureiro'` aplicado nos fetchers de exibição (`nomeDe`, `perfilDe`, `perfis`, cabeçalho, `carregar`, `salvarPerfil`) — apelido vira o nome no site todo, com fallback. Perfil ganhou campo **Apelido** e bloco “Como ser convidado” (mostra o e-mail do usuário). `renderMembros`: o `<select>` de todos os usuários VIROU **caixa de busca** (`buscarJogadores` via rpc) com resultados (avatar+apelido) e botão **✉ Convidar** (marca “já é membro”/“convite enviado”); lista de **convites pendentes** do mestre (`renderConvitesPendentes`/`cancelarConvite`). Novo fluxo do convidado: notificação `tipo='convite'` → roteia para **#/convites** (`abrirNotif`); página `telaConvites`/`renderMeusConvites` com **Aceitar** (`aceitarConvite` rpc → vira membro, recarrega mesas, notifica o mestre) / **Recusar** (`recusarConvite`). Link **✉ Meus convites** no menu do usuário. `addJogador`/`me_addjog` viraram código morto (inofensivo). Assets bumpados `?v=20260701a`. **Verificação:** `node --check` OK, `tail -1`=`init();`, `index.html` íntegro (7 `<script>`+`</html>`), `exib()` testado (apelido/fallback/null), 14/14 replaces aplicados 1× cada. **Falta (autor):** push de `app.js`+`index.html`+`schema.sql`+`migracao-usuarios-convites.sql`; **rodar a migration** no Supabase; validação ao vivo com 2 contas nos 6 temas (definir apelido; mestre busca por e-mail/apelido e convida; convidado aceita pelo sino/#convites e vira membro; recusar; cancelar). **Fase 4 (opcional, não feita):** link/código de convite da própria mesa._

_Adendo (2026-07-01, busca aprimorada): a `buscar_usuarios` virou **paginada e por prefixo case-insensitive** em **apelido, nome OU e-mail** (digitar “moi” traz quem começa com “moi”; e-mail completo também casa). Assinatura nova `buscar_usuarios(termo, p_limit default 10, p_offset default 0)` (com `drop function if exists buscar_usuarios(text)` antes) retornando `id,nome,apelido,epiteto,avatar_url,email_mascara,total` — **e-mail nunca inteiro**, só máscara `mo•••@dominio` para o mestre confirmar; `total` via `count(*) over()` para paginar; exclui o próprio usuário; termo vazio lista todos. Front `buscarJogadores(mesaId,offset)`: página de 8, botões **‹ Anterior / Próxima ›** + “ini–fim de N”; cada linha rica = **foto de perfil** + apelido (título) + sub-linha com epíteto · nome real (se difere) · e-mail mascarado; convidar mantém a página atual. `schema.sql`+`migracao-usuarios-convites.sql` atualizados. Assets `?v=20260701b`. `node --check` OK, `tail -1`=`init();`, index íntegro._

_Adendo (2026-07-01, busca ao vivo + área dedicada): (1) **busca ao vivo** na caixa de convidar — `buscaDebounce()` (oninput, 280ms) mostra os perfis abaixo do campo enquanto digita; ao esvaziar, some. (2) **Nova área dedicada “Jogadores da Mesa”** (rota `jogadores`/`telaJogadores`): botão na tela da mesa (só mestre, ao lado de Área do Mestre/Editar mesa) abre a tela que reúne **Membros e convites** (`renderMembros`: busca+convidar, convites pendentes, membros com remover) e **Pedidos de acesso** (`renderPedidos` com estado vazio “Nenhum pedido pendente”). A seção de jogadores em **Editar mesa** virou um **link** para a nova área (sem duplicação de ids). Restrita a mestre/admin. Só front, sem migration. Assets `?v=20260701d`. `node --check` OK, `tail -1`=`init();`, index íntegro. **Confirmado pelo autor: “Tudo certo”.** Requer apenas: push do front + (se ainda não) rodar `migracao-usuarios-convites.sql` (versão paginada da `buscar_usuarios`).

_Última rodada (2026-07-01, parte 2 — bugs de listas pessoais + reorganização da lateral + tela "TUDO MEU!"): **Só front (`app.js`+`estilo.css`+`index.html`), SEM migration.** **(BUG 1) Mesa removida sumindo de "Suas mesas":** causa = a RLS `mesa_select` deixa o **dono do mundo** (`is_dono_mundo`)/admin ver TODAS as mesas do mundo, então o autor (dono) via a mesa mesmo depois de sair dela. Fix: passei a carregar a **participação real** do usuário — `carregarMinhasMesas()` popula `S.papelMesa[mesa_id]=papel` de `mesa_membros` filtrado por `user_id` (chamado em `carregar()`); helper `souMembroMesa(id)`. As listas **pessoais** (TUDO MEU) filtram por participação, não por visibilidade de dono. A home do mundo renomeou o bloco "Suas mesas"/"Mesas e Campanhas" → **"Mesas do Mundo"** (lista as mesas do mundo, papel de descoberta). **(BUG 2/3) "Continuar de onde parou" mostrando apagados/rascunhos:** `visitasRecentes` só lia o `localStorage` (`mds_recent`), sem checar o banco. Nova `visitasValidas(mundoId)` (async) valida cada item contra as tabelas (`publicacoes`/`personagens`/`jornais`/`mesas` via `.in("id",...)`), **descarta apagados** (não retornados) e **rascunhos** (`estado==='rascunho'`), atualiza o título e **poda o `localStorage`** (auto-cura). `telaHome` e `telaMundoHome` passaram a `await visitasValidas(...)`. Card unificado em `cardVisita(v,mostrarMundo)` (selo `.rec-mundo` com o nome do mundo). **(MELHORIA) Lateral reorganizada + colapsável:** helper `grp(key,titulo,corpo)`+`toggleGrp(key)` — cada seção com h4 vira **colapsável** (seta ▾, estado salvo em `localStorage["mds_grp:<key>"]`). Grupos: `explorar` (Enciclopédia/Mapas/Jornais/Linha/Personagens), `mesas` ("Mesas do Mundo"), `pessoal`. Removida a antiga seção **"Área do Mestre"** da lateral (a Área do Mestre por mesa segue acessível pelo botão dentro da mesa) e a antiga seção "Criar" (migrou p/ dentro do TUDO MEU). Novo link **"TUDO MEU!"** (`linkPessoal()`) na lateral (home e mundo). **(MELHORIA) Tela `telaTudoMeu` (rota `tudoMeu`):** dashboard pessoal — cabeçalho com foto/apelido/epíteto + ações (Minha página, Editar perfil, Meus convites); **seletor de mundo** (`<select>`→`trocarMundoTudoMeu(id)` que troca o mundo em foco e mantém a tela); **"Continuar de onde parou" GLOBAL** (todos os mundos, com selo do mundo em cada card, via `visitasValidas()` sem filtro); e, para o mundo em foco: bloco **Criar** (todos os botões de criação), **Mesas que eu mestro** (`mestreDe||papel==='mestre'`), **Mesas que eu participo** (membro e não-mestre), **Meus rascunhos** (banco+local). Escopo do conteúdo por-mundo = mundo em foco (decisão do autor); recentes = global. **Verificação:** `node --check` OK, `tail -1`=`init();`, `index.html` íntegro (7 `<script>`+`</html>`), CSS balanceado (delta 0), **smoke-test em node (harness com stubs) 19/19** (sidebar sem "Área do Mestre"/com "Mesas do Mundo"+"TUDO MEU!"/grupos colapsáveis com estado salvo; `cardVisita` selo on/off; `visitasValidas` remove apagado+rascunho, mantém vivos, poda localStorage, filtra por mundo; split mestro/participo). Assets bumpados `?v=20260701e`. **Falta (autor):** push de `app.js`+`estilo.css`+`index.html`; validação ao vivo nos 6 temas + mobile (mesa removida some do TUDO MEU; recentes sem apagados/rascunhos; colapsar/expandir seções; trocar mundo no TUDO MEU). Sem migration._

_Adendo (2026-07-01, parte 2b — ajustes após validação do autor): **(1)** a seção pessoal da lateral (renomeada de "Pessoal" para **"Meu Espaço"**) voltou a ter os **atalhos de criação**: `linkPessoal()` agora inclui Rascunhos + **Conteúdo do mundo / Personagem / Jornal / Mesa** (quando há mundo em foco) + Criar mundo — além do link **TUDO MEU!** e Favoritos. **(2) Selo de mundo errado/desatualizado nos recentes:** `registrarVisita` gravava o mundo **em foco no momento da visita** (não o mundo real do item) e o **nome congelado** (ficava "Mundo de Skard" mesmo após renomear para "Skard"). Fix: `registrarVisita(tipo,id,titulo,mundoId)` agora recebe o `mundo_id` real do item (todas as chamadas passam `p.mundo_id`/`mesa.mundo_id`/`c.mundo_id`/`j.mundo_id`); e `visitasValidas` seleciona `mundo_id` de cada linha e resolve o **nome atual** via novo `nomeMundo(mundoId)` (busca em `S.mundos`), com fallback ao armazenado. Assim o selo mostra sempre o mundo correto e o nome vigente. **(3) Abrir conteúdo no tema errado:** ao abrir mesa/publicação/personagem/jornal cujo mundo difere do foco, novo `focarMundoDeConteudo(mundoId)` troca `S.mundo` (+ `aplicarTema`, recarrega mesas/favs) antes de montar a tela — o item abre no **tema do seu próprio mundo**. **Verificação:** `node --check` OK, `tail -1`=`init();`, index íntegro (7 `<script>`+`</html>`), CSS delta 0, **smoke-test node 15/15** (linkPessoal com todos os criares + jornal; seção "Meu Espaço"; `visitasValidas` corrige mundoId/nome para o real/atual; `focarMundoDeConteudo` troca foco e é no-op se já no mundo). Assets `?v=20260701f`. **Falta (autor):** push de `app.js`+`estilo.css`+`index.html`; validação ao vivo. Sem migration._
